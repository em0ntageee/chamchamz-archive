{
  "entities": {
    "CounterStats": {
      "title": "CounterStats",
      "description": "Statistics for visitor and lit candle counters",
      "type": "object",
      "properties": {
        "visitorCount": {
          "type": "integer"
        },
        "candleCount": {
          "type": "integer"
        }
      },
      "required": [
        "visitorCount",
        "candleCount"
      ]
    }
  },
  "firestore": {
    "rules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /{document=**} {\n      allow read, write: if true;\n    }\n  }\n}",
    "/counters/stats": {
      "schema": "CounterStats",
      "description": "The unique document storing the synchronized global counts"
    }
  }
}
