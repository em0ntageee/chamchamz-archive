{
  "entities": {
    "CounterStats": {
      "title": "CounterStats",
      "description": "Statistics for visitor, lit candle counters, and secret pledges",
      "type": "object",
      "properties": {
        "visitorCount": {
          "type": "integer"
        },
        "candleCount": {
          "type": "integer"
        },
        "pledgeCount": {
          "type": "integer"
        }
      },
      "required": [
        "visitorCount",
        "candleCount",
        "pledgeCount"
      ]
    },
    "Comment": {
      "title": "Comment",
      "description": "Letters of love sent to Chamchamz, James, or Juhoon",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "from": {
          "type": "string"
        },
        "to": {
          "type": "string"
        },
        "text": {
          "type": "string"
        },
        "timestamp": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "from",
        "to",
        "text",
        "timestamp"
      ]
    }
  },
  "firestore": {
    "rules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /{document=**} {\n      allow read, write: if true;\n    }\n  }\n}",
    "/counters/stats": {
      "schema": "CounterStats",
      "description": "The unique document storing the synchronized global counts"
    },
    "/comments/{commentId}": {
      "schema": "Comment",
      "description": "Letters and comments stored in Firestore"
    }
  }
}
